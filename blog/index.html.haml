---
layout: blog
title: Blog
title_link: /blog/
---
.row
  .large-12.columns
    %h1 Get Started 
    %br

- for post in page.posts
  .post
    %h2 
      %a{:href=>post.url}= post.title
    %h3 
      = post.date.strftime( '%d %B %Y' )
    %h4
      - for tag in post.tags
        - unless tag.nil?
          %a{:href=>tag.primary_page.url}= tag.to_s
    .content
      = post.content

= page.posts.links
